command firefox firefox

# inherit groups
sticky yes

# don't manage these windows
ignore polybar

unbind-key M-Right
unbind-key M-Left
unbind-key CM-Right
unbind-key CM-Left
unbind-key M-Return
unbind-key M-Up
unbind-key M-Down
bind-key C4-Right window-snap-right
bind-key C4-Left window-snap-left
bind-key 4-d "dmenu_run"
bind-key 4S-u "passmenu"
bind-key CS-4 "shot"
bind-key 4-Return window-lower
bind-key 4-Up window-raise
bind-key 4-Down window-lower
